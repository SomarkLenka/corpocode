// Injected by esbuild (`define`) at build time and by vitest under test.
// Declared as possibly-undefined so a plain `tsc`/IDE run without the define still typechecks.
declare const __CORPOCODE_VERSION__: string | undefined;
